# 安全说明

`secret-surface` 只映射变量名和引用位置。它不应该收集、打印或上传密钥值。

## 报告方式

如果发现安全问题，请优先通过 GitHub private advisory 报告；不可用时可直接联系维护者。

## 范围

安全相关问题包括：

- 打印密钥值而不是变量名
- 读取请求仓库根目录之外的文件
- 执行不可信工作区内容
- 把仓库数据发送到网络

漏识别某些变量名模式通常是普通 bug；除非导致密钥值暴露，否则不算安全漏洞。
