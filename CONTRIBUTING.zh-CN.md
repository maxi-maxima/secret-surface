# 贡献指南

感谢你改进 `secret-surface`。

## 开发

```bash
npm install
npm run check
node dist/cli.js demo
```

## Pull Request 要求

- 保持扫描逻辑确定、本地优先。
- 新增引用模式或发现项时，需要增加测试。
- 用户可见行为变化时，同时更新英文和中文文档。
- 不要加入遥测、云端调用或密钥值收集。
